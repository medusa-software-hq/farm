package software.medusa.farm.shared

/** GitHub label conventions Farm acts on. */
object FarmLabels {
  /** The label that opts an issue into automatic processing. */
  const val READY = "farm:ready"
}
