# A hung suite should cost a minute, not a CI slot. SEPARATE_THREAD makes the timeout pre-emptive:
# the default mode only reports a test as too slow once it finishes, which never happens if it is
# stuck on a process that will not end.
junit.jupiter.execution.timeout.default = 5 m
junit.jupiter.execution.timeout.thread.mode.default = SEPARATE_THREAD
