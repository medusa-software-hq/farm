#!/usr/bin/env bash
set -euo pipefail

# Dev launcher: runs the locally-installed runner (task install → installDist).
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
TARGET="$SCRIPT_DIR/../build/install/runner/bin/runner"
exec "$TARGET" "$@"
